set(LIBMASM_TOKENIZER_SOURCES
        postprocessor.cpp
        tokenizer.cpp
)
list(TRANSFORM LIBMASM_TOKENIZER_SOURCES PREPEND "${CMAKE_CURRENT_SOURCE_DIR}/")
set(LIBMASM_TOKENIZER_SOURCES "${LIBMASM_TOKENIZER_SOURCES}" PARENT_SCOPE)
